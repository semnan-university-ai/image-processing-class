clc;
close all;
clear all;
image = imread("../../../Benchmark/fruits.png");
imageSize=size(image);
[height,width,dim] = size(image);
imageResult = zeros(im
for k =1:dim
    for i=1:height
        for j=1:width
            if image(i,j)<=50
                
            end
        end
    end
end
    
