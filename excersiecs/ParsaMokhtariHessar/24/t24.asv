clc;
close all;
clear all;
imagepool = imread("../../../Benchmark/pool.png");
imagewatch = imread("../../../Benchmark/watch.png");
imagepool=(imagepool)
imagewatch=(imagewatch)
imagePool() = imagewatch(:,:,1) 
imageWatch = imagepool(:,:,1)
figure,imshow(imagePool);
figure,imshow(imageWatch);