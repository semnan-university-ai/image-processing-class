clc;
close all;
clear;
image = imread("../../benchmark/airplane.png");
image = imresize(image, [150 150]);
%imshow(image);

for i = 1 : 10
    if (mod(i,2) == 0)
        i;
    else
        0;
    end
end