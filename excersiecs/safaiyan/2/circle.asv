clc;
close all;
clear all;

img = zeros(1000,1000);
R = input('radius= ');
xc = input('x center= ');
yc = input('y center= ');
imshow(img)
hold on
Theta = linespace(0,2*pi);
x = R*cos(Theta) +xc;
y = R*sin(Theta) +yc;
plot(xc,yc,'r*')
plot(x,y,'w','LineWidth',5)